import React from 'react';
import { OCEAN_QUESTION_POOL } from '../constants';

export const OceanGameMap = () => {
    console.log(OCEAN_QUESTION_POOL);
    return <div>Ocean Game Map</div>;
};
